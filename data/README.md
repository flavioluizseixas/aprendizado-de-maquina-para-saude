# Dados e cache

Os notebooks baixam dados públicos diretamente das fontes documentadas. Bases completas não são versionadas.

`data/cache/` recebe respostas da API do InfoDengue durante a sessão. Remover o cache força um novo download; nunca coloque dados identificáveis neste diretório.

